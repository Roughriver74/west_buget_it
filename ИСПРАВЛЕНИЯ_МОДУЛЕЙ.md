# Исправление ошибок и настройка управления модулями

## ✅ Что было исправлено

### 1. Ошибка `TypeError: rawData.some is not a function`

**Проблема**: 
Страница управления модулями (`ModuleSettingsPage.tsx`) не существовала, из-за чего возникала ошибка при попытке открыть раздел.

**Решение**:
- ✅ Создана страница `/frontend/src/pages/ModuleSettingsPage.tsx`
- ✅ Добавлен маршрут `/settings/modules` (только для ADMIN)
- ✅ Добавлен пункт меню "Модули" в боковое меню администратора
- ✅ Исправлена обработка данных таблицы - теперь корректно работает с массивами

### 2. Включена настройка модулей

Теперь администратор может:
- 📊 Просматривать список всех модулей системы
- ✅ Включать/отключать модули для организаций
- 📅 Устанавливать срок действия модулей
- 🔧 Настраивать лимиты использования (в формате JSON)
- 📈 Видеть статистику по использованию модулей

## 🚀 Как использовать

### Шаг 1: Загрузить модули в БД (один раз)

```bash
cd backend
python scripts/seed_modules.py
# Нажмите 'y' для подтверждения
```

Это создаст 10 модулей:
1. **BUDGET_CORE** - Базовая функциональность (всегда включен)
2. **AI_FORECAST** - AI & прогнозирование
3. **CREDIT_PORTFOLIO** - Кредитный портфель
4. **REVENUE_BUDGET** - Бюджет доходов
5. **PAYROLL_KPI** - Система KPI
6. **HR_DEPARTMENT** - Табель
7. **INTEGRATIONS_1C** - Интеграция с 1С
8. **FOUNDER_DASHBOARD** - Дашборд учредителя
9. **ADVANCED_ANALYTICS** - Расширенная аналитика
10. **MULTI_DEPARTMENT** - Мультиотдельность

### Шаг 2: Открыть страницу управления

1. Войдите как **ADMIN**
2. В боковом меню нажмите **"Модули"** (после "Пользователи")
3. Откроется страница управления модулями

### Шаг 3: Включить/отключить модуль

**Включить модуль:**
1. Найдите модуль в таблице
2. Нажмите кнопку **"Включить"**
3. (Опционально) Установите дату окончания
4. (Опционально) Добавьте лимиты:
   ```json
   {
     "max_users": 100,
     "max_contracts": 50
   }
   ```
5. Нажмите **"Включить"**

**Отключить модуль:**
1. Найдите включенный модуль
2. Нажмите **"Отключить"**
3. Подтвердите действие

## 📋 Интерфейс страницы

### Статистика (верхняя панель)
- 📊 **Всего модулей** - количество доступных модулей
- ✅ **Активных модулей** - сколько модулей активно
- 🔓 **Включено для организации** - сколько модулей включено
- 🏢 **Всего организаций** - количество организаций в системе

### Таблица модулей
Колонки:
- **Код** - уникальный код модуля (например, AI_FORECAST)
- **Название** - человекочитаемое название
- **Описание** - что делает модуль
- **Версия** - версия модуля
- **Активен** - активен ли модуль в системе
- **Включен для организации** - включен ли для текущей организации
  - Показывает дату окончания, если установлена
- **Всего организаций** - сколько организаций используют модуль
- **Действия** - кнопки включить/отключить

## 🔧 Технические детали

### Измененные файлы

**Frontend:**
- ✅ Создан: `/frontend/src/pages/ModuleSettingsPage.tsx` (новый файл)
- ✅ Изменен: `/frontend/src/AppRoutes.tsx` (добавлен маршрут)
- ✅ Изменен: `/frontend/src/components/common/AppLayout.tsx` (добавлен пункт меню)

**Backend:**
- ✅ Существует: `/backend/app/api/v1/modules.py` (API endpoints)
- ✅ Существует: `/backend/scripts/seed_modules.py` (скрипт загрузки)

### API Endpoints используемые страницей

```
GET  /api/v1/modules/                          - Список всех модулей
GET  /api/v1/modules/statistics/               - Статистика
GET  /api/v1/modules/enabled/{organization_id} - Включенные модули
POST /api/v1/modules/enable                    - Включить модуль
POST /api/v1/modules/disable                   - Отключить модуль
GET  /api/v1/organizations/                    - Список организаций
```

## 📱 Примеры использования

### Пример 1: Включить AI модуль на 1 год

В модальном окне:
- **Дата окончания**: `31.12.2025`
- **Лимиты**: 
  ```json
  {
    "max_ocr_per_month": 1000,
    "max_classifications_per_day": 500
  }
  ```

### Пример 2: Включить Revenue Budget без ограничений

В модальном окне:
- **Дата окончания**: *(оставить пустым)*
- **Лимиты**: `{}` *(или оставить пустым)*

### Пример 3: Включить KPI систему с лимитом пользователей

В модальном окне:
- **Дата окончания**: *(оставить пустым)*
- **Лимиты**: 
  ```json
  {
    "max_employees": 500,
    "max_goals_per_employee": 10
  }
  ```

## ❓ Часто задаваемые вопросы

**Q: Нужно ли каждый раз запускать seed_modules.py?**
A: Нет, только один раз при первой настройке. При повторном запуске скрипт обновит существующие модули.

**Q: Что произойдет, если я отключу модуль?**
A: Функционал модуля станет недоступен для организации. Данные не удаляются.

**Q: Можно ли включить модуль для нескольких организаций?**
A: Да, но пока только по одной. Пока нет массовых операций.

**Q: Что такое лимиты и когда их использовать?**
A: Лимиты - это ограничения на использование функций модуля (например, максимум 100 договоров). Используйте их для контроля ресурсов.

**Q: Можно ли продлить срок действия модуля?**
A: Да, просто включите модуль заново с новой датой окончания.

**Q: Где посмотреть историю включения/отключения?**
A: История хранится в таблице `module_events`. Пока нет UI, но можно посмотреть в БД.

## 🐛 Устранение проблем

### Ошибка: "Module not found"
**Решение**: Запустите `python scripts/seed_modules.py`

### Ошибка: "403 Forbidden"
**Решение**: Войдите как пользователь с ролью ADMIN

### Модуль не появляется после включения
**Решение**: 
1. Обновите страницу (F5)
2. Перелогиньтесь

### Страница не загружается
**Решение**:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что backend запущен
3. Проверьте сетевые запросы в DevTools

## 📚 Дополнительная документация

Подробная инструкция: [MODULE_SETTINGS_SETUP.md](MODULE_SETTINGS_SETUP.md)

Полная документация по модулям: [docs/MODULES.md](docs/MODULES.md)

---

✅ **Все исправлено и готово к использованию!**

Если возникнут вопросы - обращайтесь к документации выше или к разработчикам.
