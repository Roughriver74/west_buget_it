name: Deploy to Test Server

on:
  push:
    branches:
      - test
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: test

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.TEST_SERVER_SSH_KEY }}

      - name: Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.TEST_SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Deploy to test server
        env:
          SERVER_IP: ${{ secrets.TEST_SERVER_IP }}
          SERVER_USER: ${{ secrets.TEST_SERVER_USER }}
          PROJECT_PATH: ${{ secrets.TEST_SERVER_PROJECT_PATH }}
          GITHUB_REPO: ${{ github.repository }}
        run: |
          ssh $SERVER_USER@$SERVER_IP "export PROJECT_PATH='$PROJECT_PATH'; export GITHUB_REPO='$GITHUB_REPO'; bash -s" << 'ENDSSH'
            set -e
            echo "🚀 Starting deployment to test server..."
            echo "📁 Project path: $PROJECT_PATH"

            # Validate PROJECT_PATH is set
            if [ -z "$PROJECT_PATH" ]; then
              echo "❌ Error: PROJECT_PATH is not set!"
              exit 1
            fi

            # Navigate to project directory or create it
            if [ ! -d "$PROJECT_PATH" ]; then
              echo "📁 Creating project directory..."
              mkdir -p "$PROJECT_PATH"
            fi
            
            cd "$PROJECT_PATH" || exit 1

            # Check if git repository exists, if not - clone or initialize
            if [ ! -d .git ]; then
              echo "📥 Git repository not found. Checking if directory is empty..."
              if [ "$(ls -A 2>/dev/null)" ]; then
                echo "⚠️  Directory is not empty. Initializing git repository..."
                git init
                git remote add origin "git@github.com:$GITHUB_REPO.git" 2>/dev/null || git remote set-url origin "git@github.com:$GITHUB_REPO.git"
                git fetch origin test
                git checkout -b test origin/test || true
              else
                echo "📥 Cloning repository..."
                git clone -b test "git@github.com:$GITHUB_REPO.git" .
              fi
            else
              # Ensure remote is set
              if ! git remote get-url origin &>/dev/null; then
                git remote add origin "git@github.com:$GITHUB_REPO.git"
              fi
              
              # Pull latest changes from test branch
              echo "📥 Pulling latest changes..."
              git fetch origin test || {
                echo "⚠️  Failed to fetch, trying to set remote..."
                git remote set-url origin "git@github.com:$GITHUB_REPO.git"
                git fetch origin test
              }
              git reset --hard origin/test || git checkout -b test origin/test
            fi

            # Stop containers
            echo "🛑 Stopping containers..."
            docker compose -f docker-compose.test.yml down || true

            # Rebuild and start containers
            echo "🔨 Building and starting containers..."
            docker compose -f docker-compose.test.yml up -d --build

            # Wait for services to be healthy
            echo "⏳ Waiting for services to start..."
            sleep 30

            # Check service status
            echo "✅ Checking service status..."
            docker compose -f docker-compose.test.yml ps

            # Test backend health
            echo "🏥 Testing backend health..."
            curl -f http://localhost:8889/health || echo "⚠️  Backend health check failed"

            echo "✅ Deployment completed!"
          ENDSSH

      - name: Deployment status
        run: |
          echo "✅ Deployment to test server completed successfully!"
          echo "🌐 Frontend: http://${{ secrets.TEST_SERVER_IP }}:3002"
          echo "🔧 Backend API: http://${{ secrets.TEST_SERVER_IP }}:8889"
          echo "📚 API Docs: http://${{ secrets.TEST_SERVER_IP }}:8889/docs"
