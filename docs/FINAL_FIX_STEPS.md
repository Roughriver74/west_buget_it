# ✅ ФИНАЛЬНЫЕ ШАГИ ДЛЯ ИСПРАВЛЕНИЯ

## Что было сделано

1. ✅ Найдена причина: в старой версии Dockerfile.prod был хардкод `http://localhost:8888`
2. ✅ Исправлен Dockerfile.prod с правильным URL: `https://api.budget-west.shknv.ru/api/v1`
3. ✅ Создан .dockerignore для чистой сборки
4. ✅ Обновлён docker-compose.prod.yml для гибкой конфигурации
5. ✅ Закоммичено и запушено в Git (коммит: b7f6756)

---

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС в Docker

### Шаг 1: Откройте Docker

Зайдите на https://west-it.ru (или http://93.189.228.52:8000)

### Шаг 2: Найдите Frontend приложение

В проекте "IT Budget Manager" откройте **Frontend** сервис

### Шаг 3: Очистите кэш сборки (ВАЖНО!)

⚠️ **КРИТИЧЕСКИ ВАЖНО**: Нужно очистить Docker build cache!

Есть несколько способов:

#### Способ 1: В интерфейсе Docker (если есть опция)

Найдите кнопку:
- "Clear Build Cache"
- "Clean Build"
- "Rebuild from scratch"

#### Способ 2: Через SSH (если нет кнопки в UI)

```bash
ssh root@93.189.228.52

# Найдите frontend контейнер и образ
docker ps | grep frontend
docker images | grep frontend

# Остановите и удалите контейнер
docker stop <frontend_container_id>
docker rm <frontend_container_id>

# Удалите образ (это очистит кэш)
docker rmi <frontend_image_id> -f

# Опционально: очистите все неиспользуемые build кэши
docker builder prune -a -f
```

### Шаг 4: Rebuild в Docker

1. в Docker для Frontend нажмите **"Deploy"** или **"Rebuild"**
2. Docker автоматически:
   - Подтянет последний код из Git (коммит b7f6756)
   - Пересоберёт без старого кэша
   - Использует правильный URL из Dockerfile.prod
3. Дождитесь окончания сборки (3-5 минут)

### Шаг 5: Проверка

После деплоя выполните проверки:

#### 5.1. Проверьте конфигурацию

```bash
curl https://budget-west.shknv.ru/config-check
```

**Ожидаемый результат:**
```javascript
window.ENV_CONFIG = {
  VITE_API_URL: 'https://api.budget-west.shknv.ru/api/v1'
};
```

❌ Если всё ещё показывает `http://localhost:8888` - значит:
- Кэш не очистился
- Docker использует старый образ
- Нужно удалить образ через SSH (см. Способ 2 выше)

#### 5.2. Проверьте API

```bash
curl https://api.budget-west.shknv.ru/api/v1/health
```

**Ожидаемый результат:**
```json
{"status":"ok"}
```

#### 5.3. Проверьте в браузере

1. **Очистите кэш браузера**: Ctrl+Shift+Delete (или Cmd+Shift+Delete на Mac)
2. Откройте https://budget-west.shknv.ru/login
3. Откройте DevTools (F12) → **Console** tab
4. Попробуйте залогиниться (admin / !QAZ2wsx)
5. **НЕ должно быть ошибок** про `localhost:8888` или CORS

#### 5.4. Проверьте Network tab

В DevTools → **Network** tab:
- ✅ Должны быть запросы к `https://api.budget-west.shknv.ru/api/v1/auth/login`
- ❌ НЕ должно быть запросов к `localhost:8888`

---

## 🔍 Если всё ещё не работает

### Проблема: env-config.js показывает localhost:8888

**Это значит что используется старая сборка с кэшем.**

**Решение:**
```bash
ssh root@93.189.228.52

# Полная очистка frontend
docker stop $(docker ps -q -f name=frontend)
docker rm $(docker ps -aq -f name=frontend)
docker rmi $(docker images -q -f reference='*frontend*') -f

# Очистка всех build кэшей
docker builder prune -a -f

# После этого в Docker нажмите Deploy
```

### Проблема: CORS ошибки

**Проверьте Backend:**

```bash
# Подключитесь к backend контейнеру
docker exec -it $(docker ps -q -f name=backend) env | grep CORS

# Должно показать:
# CORS_ORIGINS=["https://budget-west.shknv.ru","https://api.budget-west.shknv.ru"]
```

Если CORS_ORIGINS неправильный:
1. в Docker откройте Backend
2. Environment Variables
3. Проверьте `CORS_ORIGINS=["https://budget-west.shknv.ru","https://api.budget-west.shknv.ru"]`
4. Restart Backend

### Проблема: 404 на API

```bash
curl https://api.budget-west.shknv.ru/api/v1/health
```

Если получаете 404:
- Проверьте что Backend запущен: `docker ps | grep backend`
- Проверьте логи Backend: `docker logs $(docker ps -q -f name=backend) --tail 50`
- Проверьте что Docker правильно настроил proxy для домена `api.budget-west.shknv.ru`

---

## 📋 Чеклист финальной проверки

- [ ] Очистил Docker build cache
- [ ] Сделал Rebuild в Docker
- [ ] Проверил `curl https://budget-west.shknv.ru/config-check` - показывает правильный URL
- [ ] Проверил `curl https://api.budget-west.shknv.ru/api/v1/health` - возвращает {"status":"ok"}
- [ ] Очистил кэш браузера
- [ ] Открыл сайт и проверил Console - нет ошибок про localhost:8888
- [ ] Залогинился успешно
- [ ] Проверил Network tab - все запросы идут на правильный домен

---

## 🎯 Почему это должно сработать?

### Исправлено:

1. **В Dockerfile.prod теперь правильный дефолт:**
   ```dockerfile
   ARG VITE_API_URL=https://api.budget-west.shknv.ru/api/v1
   ```

2. **Добавлен .dockerignore** - не копируются локальные .env файлы

3. **docker-compose.prod.yml гибкий** - можно переопределить через env vars

4. **Изменения в Git** - Docker подтянет свежий код

### Что происходит при сборке:

```
1. Docker клонирует Git репозиторий (коммит b7f6756)
2. Читает Dockerfile.prod с VITE_API_URL=https://api.budget-west.shknv.ru/api/v1
3. Собирает frontend с этим URL встроенным в код
4. При старте контейнера docker-entrypoint.sh создаёт env-config.js
5. Браузер загружает правильный URL
```

### Почему раньше не работало:

- Docker использовал **кэшированный** layer с `ARG VITE_API_URL=http://localhost:8888`
- Даже при rebuild кэш не очищался автоматически
- Environment Variables в Docker **не переопределяли** ARG при наличии кэша

---

## 📞 Если нужна дополнительная помощь

Пришлите вывод следующих команд:

```bash
# 1. Версия кода на сервере
ssh root@93.189.228.52
cd /path/to/app  # путь к коду на сервере
git log --oneline -5

# 2. Текущая конфигурация
curl https://budget-west.shknv.ru/config-check

# 3. Образы Docker
docker images | grep budget

# 4. Логи сборки (если есть)
docker logs $(docker ps -q -f name=frontend) --tail 100
```

---

## ✨ После успешного деплоя

Всё должно работать! Вы сможете:
- ✅ Залогиниться через https://budget-west.shknv.ru
- ✅ API будет доступен через https://api.budget-west.shknv.ru
- ✅ Нет ошибок CORS или mixed content
- ✅ Все запросы идут на правильные домены

**Данные для входа:**
- Username: `admin`
- Password: `!QAZ2wsx`
