# Справочник статусов банковских транзакций

## Статусы обработки (BankTransactionStatusEnum)

### 🆕 NEW - Новая транзакция
**Описание**: Транзакция только что импортирована и еще не обработана системой.

**Когда устанавливается**:
- При ручном импорте из Excel (если `auto_classify=false`)
- При автоматическом импорте через FTP (до обработки)
- При создании транзакции через API

**Следующие возможные статусы**:
- `CATEGORIZED` - после назначения категории
- `MATCHED` - после связывания с заявкой
- `NEEDS_REVIEW` - если AI не смог определить категорию с высокой уверенностью
- `IGNORED` - если транзакция не относится к учету

**Действия пользователя**:
- Назначить категорию вручную
- Связать с заявкой на расход
- Проигнорировать транзакцию

---

### 📋 CATEGORIZED - Категоризирована
**Описание**: Транзакции назначена категория расходов (статья бюджета).

**Когда устанавливается**:
- После успешной AI-классификации (confidence > 90%)
- После ручного назначения категории финансистом
- После массовой категоризации (bulk-categorize)

**Данные**:
- `category_id` - ID назначенной категории
- `category_confidence` - уровень уверенности AI (0-1), может быть NULL для ручного назначения

**Следующие возможные статусы**:
- `MATCHED` - после связывания с заявкой
- `APPROVED` - после проверки и утверждения
- `NEEDS_REVIEW` - если требуется пересмотр

**Действия пользователя**:
- Связать с заявкой на расход (опционально)
- Утвердить транзакцию
- Изменить категорию

---

### 🔗 MATCHED - Связана с заявкой
**Описание**: Транзакция связана с конкретной заявкой на расход (Expense).

**Когда устанавливается**:
- После успешного smart-matching (matching_score > 85)
- После ручного связывания финансистом
- После массового связывания (bulk-link)

**Данные**:
- `expense_id` - ID связанной заявки
- `matching_score` - степень совпадения (0-100)
- `category_id` - категория (может быть автоматически взята из заявки)

**Следующие возможные статусы**:
- `APPROVED` - после проверки и утверждения
- `CATEGORIZED` - если связь разорвана

**Действия пользователя**:
- Утвердить транзакцию
- Разорвать связь с заявкой
- Добавить примечания

---

### ✅ APPROVED - Одобрена
**Описание**: Транзакция проверена и одобрена финансистом. Финальный статус.

**Когда устанавливается**:
- После ручного утверждения финансистом
- После массового утверждения (bulk-status-update)

**Данные**:
- `reviewed_by` - ID пользователя, который утвердил
- `reviewed_at` - дата и время утверждения
- `notes` - примечания финансиста (опционально)

**Следующие возможные статусы**:
- Нет (финальный статус)
- Можно вернуть в `NEEDS_REVIEW` при необходимости пересмотра

**Действия пользователя**:
- Использование в отчетах и аналитике
- При необходимости - вернуть на пересмотр

---

### ⚠️ NEEDS_REVIEW - Требует проверки
**Описание**: Транзакция требует ручной проверки финансистом.

**Когда устанавливается**:
- После AI-классификации с низкой уверенностью (confidence < 90%)
- После smart-matching с низким score (< 85)
- При обнаружении аномалий (необычная сумма, новый контрагент и т.д.)
- После ручного возврата из `APPROVED`

**Данные**:
- `suggested_category_id` - предложенная AI категория (если есть)
- `suggested_expense_id` - предложенная заявка (если есть)
- `category_confidence` - уровень уверенности AI

**Следующие возможные статусы**:
- `CATEGORIZED` - после назначения категории
- `MATCHED` - после связывания с заявкой
- `APPROVED` - после утверждения (редко, обычно сначала категоризируется)
- `IGNORED` - если не относится к учету

**Действия пользователя**:
- Рассмотреть предложения AI
- Назначить правильную категорию
- Связать с заявкой (если нужно)
- Утвердить или проигнорировать

---

### 🚫 IGNORED - Проигнорирована
**Описание**: Транзакция не относится к учету и должна быть проигнорирована.

**Когда устанавливается**:
- При ручном исключении финансистом
- Для возвратов, ошибочных операций, внутренних переводов и т.д.

**Примеры**:
- Возврат ошибочного платежа
- Внутренние переводы между счетами
- Комиссии банка (если учитываются отдельно)
- Тестовые платежи

**Следующие возможные статусы**:
- Можно вернуть в `NEW` или `NEEDS_REVIEW` при ошибочном игнорировании

**Действия пользователя**:
- Добавить примечание о причине игнорирования
- При необходимости - вернуть в обработку

---

## Workflow статусов

```
       NEW
        ↓
   ┌────┴────┐
   ↓         ↓
CATEGORIZED  NEEDS_REVIEW ←──┐
   ↓         ↓               │
   └────┬────┘               │
        ↓                    │
     MATCHED                 │
        ↓                    │
     APPROVED ───────────────┘

   IGNORED (из любого статуса)
```

## Статистика по статусам

**Получить статистику**:
```bash
GET /api/v1/bank-transactions/stats?department_id=1
```

**Ответ**:
```json
{
  "total": 1500,
  "by_status": {
    "NEW": 45,
    "CATEGORIZED": 180,
    "MATCHED": 320,
    "APPROVED": 850,
    "NEEDS_REVIEW": 85,
    "IGNORED": 20
  },
  "by_type": {
    "DEBIT": 1200,
    "CREDIT": 300
  },
  "total_amount": 15750000.00,
  "unprocessed_count": 130
}
```

## Фильтрация по статусу

**Получить транзакции определенного статуса**:
```bash
GET /api/v1/bank-transactions?status=NEEDS_REVIEW&department_id=1
```

**Получить только необработанные (NEW + NEEDS_REVIEW)**:
```bash
GET /api/v1/bank-transactions?only_unprocessed=true&department_id=1
```

## Массовые операции

### Массовое обновление статуса
```bash
POST /api/v1/bank-transactions/bulk-status-update
{
  "transaction_ids": [1, 2, 3, 4, 5],
  "status": "APPROVED"
}
```

### Массовая категоризация
```bash
POST /api/v1/bank-transactions/bulk-categorize
{
  "transaction_ids": [1, 2, 3],
  "category_id": 15
}
```

## Рекомендации по работе

### Для финансистов
1. **Ежедневно проверяйте `NEEDS_REVIEW`** - это транзакции, требующие вашего внимания
2. **Используйте `only_unprocessed=true`** - для быстрого доступа к необработанным
3. **Добавляйте примечания** в поле `notes` для важных транзакций
4. **Утверждайте пакетами** - используйте bulk-операции для однотипных транзакций

### Для администраторов
1. **Мониторьте количество `NEW`** - большое количество может означать проблемы с импортом
2. **Анализируйте `NEEDS_REVIEW`** - частые попадания определенных типов транзакций означают необходимость улучшения ключевых слов
3. **Проверяйте регулярные платежи** - `is_regular_payment=true` должны автоматически категоризироваться

## Аудит и отчетность

Все изменения статусов логируются в `audit_logs`:
- Кто изменил статус
- Когда изменил
- Предыдущий и новый статус
- Причина изменения (если указана)

**Получить историю изменений**:
```python
from app.db.models import AuditLog, AuditActionEnum

logs = db.query(AuditLog).filter(
    AuditLog.entity_type == "bank_transaction",
    AuditLog.entity_id == transaction_id,
    AuditLog.action == AuditActionEnum.UPDATE
).order_by(AuditLog.created_at.desc()).all()
```
