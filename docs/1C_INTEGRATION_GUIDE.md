# Руководство по интеграции с 1С

## 🆕 Последние обновления

### Версия 2.0 - Новые возможности (2025-11-05)

**✨ Добавлены новые endpoint'ы:**
- `GET /pending?only_not_created_in_1c=true` - фильтр только новых счетов
- `GET /{invoice_id}` - получение полной информации включая табличную часть
- `POST /{invoice_id}/mark-created-in-1c` - отметка о создании в 1С

**📊 Новые поля в ответах:**
- `external_id_1c` - ID документа в 1С
- `created_in_1c_at` - дата/время создания в 1С
- `parsed_data.items[]` - табличная часть счета (позиции с количеством и ценой)
- `contract_number` и `contract_date` - данные договора

**🎯 Преимущества новой версии:**
- Полная информация о позициях счета для автоматического заполнения документов
- Отслеживание статуса обработки в 1С
- Исключение дублирования через фильтрацию уже обработанных счетов
- Двунаправленная связь документов (invoice_id ↔ external_id_1c)

## Обзор

Система обработки счетов с использованием AI интегрирована с 1С для автоматического создания заявок на расходование денежных средств.

### Архитектура интеграции

```
[Веб-интерфейс] → [OCR + AI Parser] → [ProcessedInvoice]
                                              ↓
                                    [External API] ← [1С:Предприятие]
                                              ↓
                                         [Expense]
```

## 1. Подготовка API

### 1.1 Настройка департамента

**ВАЖНО:** Перед началом интеграции необходимо настроить соответствие между департаментами системы и подразделениями 1С.

1. Войдите в систему как администратор
2. Перейдите в раздел **"Департаменты"**
3. Откройте департамент для редактирования
4. В поле **"Подразделение (для FTP импорта)"** укажите код подразделения из 1С
   - Например: `(ДЕМО) IT` или `ACME_IT`
   - Этот код будет использоваться для связи данных между системами
5. Сохраните изменения

**Пример настройки:**
```
Название отдела: IT Отдел ACME
Код отдела: ACME_IT
Подразделение (для FTP импорта): (ДЕМО) IT
```

**Примечание:** Код подразделения должен точно соответствовать названию подразделения в справочнике 1С. Это обеспечит корректную работу автоматической привязки счетов к нужному подразделению.

### 1.2 Создание API токена

1. Войдите в систему как администратор
2. Перейдите в раздел **"API Tokens"** (доступен только для ADMIN)
3. Нажмите **"Создать токен"**
4. Заполните форму:
   - **Название**: `1C Integration Token`
   - **Scope**: `FULL_ACCESS` (для чтения и записи)
   - **Expires At**: Установите срок действия (рекомендуется 1 год)
5. **ВАЖНО**: Скопируйте токен сразу после создания! Он больше не будет показан.

### 1.2 API Endpoints

Все endpoints находятся по пути: `http://your-server:8000/api/v1/external/invoices`

#### Получить список счетов для обработки (список - краткая информация)
```http
GET /api/v1/external/invoices/pending
Authorization: Bearer {api_token}

Query параметры:
- department_id (optional): ID департамента для фильтрации
- only_not_created_in_1c (optional): true - только счета, еще не созданные в 1С (default: false)
- limit (optional): Максимум записей (default: 100, max: 500)
```

**Пример запроса:**
```http
GET /api/v1/external/invoices/pending?only_not_created_in_1c=true&department_id=1&limit=50
Authorization: Bearer your_api_token_here
```

**Ответ:**
```json
[
  {
    "invoice_id": 123,
    "invoice_number": "71",
    "invoice_date": "2025-10-31",
    "supplier_name": "ИП Серебряков Евгений Владимирович",
    "supplier_inn": "744915834349",
    "total_amount": 14500.0,
    "department_id": 1,
    "processed_at": "2025-01-15T10:30:00",
    "external_id_1c": null,
    "created_in_1c_at": null
  }
]
```

**Примечание:** Используйте `only_not_created_in_1c=true` для получения только тех счетов, которые еще не были созданы в 1С.

#### Получить полную информацию о счете (включая items)
```http
GET /api/v1/external/invoices/{invoice_id}
Authorization: Bearer {api_token}
```

**Пример запроса:**
```http
GET /api/v1/external/invoices/123
Authorization: Bearer your_api_token_here
```

**Ответ:**
```json
{
  "invoice_id": 123,
  "invoice_number": "71",
  "invoice_date": "2025-10-31",
  "supplier_name": "ИП Серебряков Евгений Владимирович",
  "supplier_inn": "744915834349",
  "supplier_kpp": null,
  "supplier_bank_name": "АО \"ТИНЬКОФФ БАНК\"",
  "supplier_bik": "044525974",
  "supplier_account": "40802810400001234567",
  "total_amount": 14500.0,
  "amount_without_vat": 14500.0,
  "vat_amount": 0,
  "payment_purpose": "Оплата по счету №71 от 31.10.2025",
  "contract_number": null,
  "contract_date": null,
  "department_id": 1,
  "original_filename": "invoice_71.pdf",
  "processed_at": "2025-01-15T10:30:00",
  "external_id_1c": null,
  "created_in_1c_at": null,
  "parsed_data": {
    "invoice_number": "71",
    "invoice_date": "2025-10-31",
    "supplier": {
      "name": "ИП Серебряков Евгений Владимирович",
      "inn": "744915834349",
      "kpp": null,
      "bank_name": "АО \"ТИНЬКОФФ БАНК\"",
      "bik": "044525974",
      "account": "40802810400001234567",
      "corr_account": "30101810145250000974"
    },
    "amount_without_vat": "14500.0",
    "vat_amount": "0",
    "total_amount": "14500.0",
    "payment_purpose": "Оплата по счету №71 от 31.10.2025",
    "contract_number": null,
    "contract_date": null,
    "items": [
      {
        "description": "Разработка веб-приложения",
        "quantity": "1",
        "unit": "шт",
        "price": "14500.0",
        "amount": "14500.0"
      }
    ]
  },
  "ocr_text": "полный текст OCR...",
  
}
```

**Ключевые поля:**
- `parsed_data` - полная структура данных счета с табличной частью (items)
- `parsed_data.items` - массив позиций счета с количеством, ценой и суммой
- `external_id_1c` - ID документа в 1С (null если еще не создан)
- `created_in_1c_at` - дата/время создания в 1С (null если еще не создан)

#### Проверить статус счета
```http
GET /api/v1/external/invoices/{invoice_id}/status
Authorization: Bearer {api_token}
```

**Ответ:**
```json
{
  "invoice_id": 123,
  "status": "PROCESSED",
  "expense_created": false,
  "expense_id": null,
  "expense_number": null,
  "expense_status": null,
  "processed_at": "2025-01-15T10:30:00"
}
```

#### Отметить счет как созданный в 1С (НОВОЕ!)
```http
POST /api/v1/external/invoices/{invoice_id}/mark-created-in-1c
Authorization: Bearer {api_token}
Content-Type: application/json

{
  "external_id_1c": "DOC-2025-001234"
}
```

**Пример запроса:**
```bash
curl -X POST "http://your-server:8000/api/v1/external/invoices/123/mark-created-in-1c" \
  -H "Authorization: Bearer your_api_token_here" \
  -H "Content-Type: application/json" \
  -d '{
    "external_id_1c": "ЗРС-00001234"
  }'
```

**Ответ:**
```json
{
  "success": true,
  "message": "Счет 123 успешно отмечен как созданный в 1С с ID ЗРС-00001234"
}
```

**Важно:**
- После создания документа в 1С обязательно вызовите этот endpoint
- Это позволит отслеживать, какие счета уже обработаны
- `external_id_1c` - номер или GUID документа в 1С (будет сохранен для связи)
- После вызова этого endpoint счет не будет возвращаться при запросе с `only_not_created_in_1c=true`

#### Создать заявку из 1С
```http
POST /api/v1/external/invoices/{invoice_id}/create-expense
Authorization: Bearer {api_token}
Content-Type: application/json

{
  "invoice_id": 123,
  "document_number_1c": "ЗРС-00001234",
  "document_date_1c": "2025-01-15T12:00:00",
  "category_id": 5,
  "amount": 14500.0,
  "comment": "Создано из 1С УТ"
}
```

**Ответ:**
```json
{
  "success": true,
  "expense_id": 456,
  "expense_number": "EXP-2025-00123",
  "contractor_id": 78,
  "contractor_created": true,
  "message": "Заявка успешно создана: EXP-2025-00123"
}
```

## 2. Настройка обработки в 1С

### 2.1 Установка обработки

1. Скопируйте файл `1c_integration/ОбработкаСчетовИзAPI.bsl`
2. В 1С УТ: **Главное меню → Сервис → Внешние обработки**
3. Нажмите **"Загрузить"** и выберите файл обработки
4. Сохраните обработку в базе

### 2.2 Конфигурация

Откройте обработку и настройте константы в начале модуля:

```bsl
// ============= НАСТРОЙКИ =============
// TODO: Получить токен из веб-интерфейса (раздел API Tokens)
АдресСервера = "http://93.189.228.52:8000";  // URL вашего сервера
APIТокен = "your-api-token-here";              // Токен из веб-интерфейса
ИДДепартамента = 1;                             // ID департамента
КоличествоЗаписей = 50;                         // Сколько счетов обрабатывать за раз
```

### 2.3 Настройка категорий

В процедуре `ПолучитьКатегориюРасхода()` настройте соответствие между категориями:

```bsl
Функция ПолучитьКатегориюРасхода(НазваниеКатегории)
    // TODO: Настроить соответствие категорий системы бюджетирования
    // и статей затрат в 1С

    Соответствие = Новый Соответствие;
    Соответствие.Вставить("Офисные расходы", 1);
    Соответствие.Вставить("IT оборудование", 2);
    // ... добавьте свои категории

    Возврат Соответствие.Получить(НазваниеКатегории);
КонецФункции
```

### 2.4 Настройка организации

В процедуре `ПолучитьОрганизацию()` настройте логику выбора организации:

```bsl
Функция ПолучитьОрганизацию(ДанныеСчета)
    // TODO: Настроить логику определения организации
    // Можно использовать:
    // - ДанныеСчета.department_id
    // - ДанныеСчета.supplier_name
    // - Реквизиты счета

    // Пример: получить основную организацию
    Запрос = Новый Запрос;
    Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ 1
    |    Организации.Ссылка КАК Организация
    |ИЗ
    |    Справочник.Организации КАК Организации
    |ГДЕ
    |    Организации.ПометкаУдаления = ЛОЖЬ";

    Результат = Запрос.Выполнить();
    Если НЕ Результат.Пустой() Тогда
        Выборка = Результат.Выбрать();
        Выборка.Следующий();
        Возврат Выборка.Организация;
    КонецЕсли;

    Возврат Неопределено;
КонецФункции
```

## 3. Тестирование интеграции

### 3.1 Проверка подключения

1. Откройте обработку в 1С
2. Нажмите **"Проверить подключение"** (если такая кнопка есть)
3. Или запустите процедуру `ПолучитьСписокСчетов()` в отладчике
4. Проверьте, что возвращается список счетов

### 3.2 Тестовая обработка

1. Загрузите тестовый счет через веб-интерфейс:
   - Перейдите в **"Заявки"**
   - Нажмите **"Обработка счетов"**
   - Загрузите PDF счет
   - Дождитесь обработки OCR + AI

2. Запустите обработку в 1С:
   - Откройте обработку
   - Нажмите **"Обработать счета"**
   - Проверьте лог обработки

3. Проверьте результат:
   - В 1С должен создаться документ **"Заявка на расходование денежных средств"**
   - В веб-интерфейсе должна появиться новая заявка с номером EXP-YYYY-XXXXX
   - Статус счета должен измениться на `EXPENSE_CREATED`

### 3.3 Проверка автоматического создания контрагентов

При создании заявки система автоматически:
1. Ищет контрагента по ИНН в указанном департаменте
2. Если не найден - создает нового контрагента с данными из счета
3. Связывает заявку с контрагентом

Проверьте в веб-интерфейсе:
- Перейдите в **"Контрагенты"**
- Найдите контрагента по ИНН из счета
- Убедитесь, что данные корректны

## 4. Автоматизация

### 4.1 Регламентное задание в 1С

Для автоматической обработки создайте регламентное задание:

1. **Конфигуратор → Метаданные → Регламентные задания**
2. Создайте новое задание:
   - **Имя**: `ОбработкаСчетовИзAPI`
   - **Метод**: `ОбработатьСчета()` из внешней обработки
   - **Расписание**: Каждые 30 минут в рабочее время

### 4.2 Мониторинг ошибок

Обработка логирует все действия. Для мониторинга:

```bsl
// В процедуре ОбработатьСчета() есть статистика:
Сообщить("=== ИТОГИ ОБРАБОТКИ ===");
Сообщить("Всего счетов получено: " + ВсегоСчетов);
Сообщить("Успешно обработано: " + УспешноОбработано);
Сообщить("Ошибок: " + Ошибок);
Сообщить("Пропущено (уже обработаны): " + Пропущено);
```

Рекомендуется сохранять эти логи в файл или таблицу регистра сведений.

## 5. Безопасность

### 5.1 Защита API токена

⚠️ **ВАЖНО**: API токен дает полный доступ к системе!

- Храните токен в защищенном месте (константы конфигурации 1С)
- Не передавайте токен третьим лицам
- Регулярно меняйте токен (рекомендуется раз в 6 месяцев)
- При компрометации немедленно удалите токен в веб-интерфейсе

### 5.2 HTTPS

В продакшене используйте HTTPS:
```bsl
АдресСервера = "https://your-domain.com";  // HTTPS вместо HTTP
```

### 5.3 Сетевой доступ

Убедитесь, что:
- Сервер 1С имеет доступ к API серверу
- Порт 8000 (или 443 для HTTPS) открыт
- Нет блокировки на файрволле

## 6. Troubleshooting

### Проблема: "Ошибка подключения к серверу"

**Решение:**
1. Проверьте доступность сервера: `ping 93.189.228.52`
2. Проверьте URL и порт в настройках обработки
3. Проверьте firewall и сетевые настройки

### Проблема: "Ошибка авторизации (401)"

**Решение:**
1. Проверьте правильность API токена
2. Убедитесь, что токен не истек (проверьте в веб-интерфейсе)
3. Проверьте, что токен имеет нужный scope (WRITE или FULL_ACCESS)

### Проблема: "Ошибка создания заявки (403)"

**Решение:**
1. Убедитесь, что токен имеет scope WRITE или FULL_ACCESS
2. Проверьте права доступа токена в веб-интерфейсе

### Проблема: "Категория не найдена"

**Решение:**
1. Проверьте соответствие категорий в `ПолучитьКатегориюРасхода()`
2. Убедитесь, что категория существует в системе бюджетирования
3. Проверьте department_id категории

### Проблема: "Контрагент не создан"

**Решение:**
1. Проверьте, что в счете есть ИНН поставщика
2. Убедитесь, что department_id корректен
3. Проверьте логи API на backend для деталей

## 7. API Reference

### Схемы данных

#### InvoiceListItem (краткая информация)
```json
{
  "invoice_id": "integer",
  "invoice_number": "string",
  "invoice_date": "string (ISO 8601)",
  "supplier_name": "string",
  "supplier_inn": "string | null",
  "total_amount": "float",
  "department_id": "integer",
  "processed_at": "string (ISO 8601)",
  "external_id_1c": "string | null",
  "created_in_1c_at": "string (ISO 8601) | null"
}
```

#### InvoiceFor1C (полная информация)
```json
{
  "invoice_id": "integer",
  "invoice_number": "string",
  "invoice_date": "string (ISO 8601)",
  "supplier_name": "string",
  "supplier_inn": "string | null",
  "supplier_kpp": "string | null",
  "supplier_bank_name": "string | null",
  "supplier_bik": "string | null",
  "supplier_account": "string | null",
  "total_amount": "float",
  "amount_without_vat": "float | null",
  "vat_amount": "float | null",
  "payment_purpose": "string | null",
  "contract_number": "string | null",
  "contract_date": "string (ISO 8601) | null",
  "department_id": "integer",
  "original_filename": "string",
  "processed_at": "string (ISO 8601)",
  "external_id_1c": "string | null",
  "created_in_1c_at": "string (ISO 8601) | null",
  "parsed_data": {
    "invoice_number": "string",
    "invoice_date": "string",
    "supplier": {
      "name": "string",
      "inn": "string | null",
      "kpp": "string | null",
      "bank_name": "string | null",
      "bik": "string | null",
      "account": "string | null",
      "corr_account": "string | null"
    },
    "amount_without_vat": "string",
    "vat_amount": "string",
    "total_amount": "string",
    "payment_purpose": "string | null",
    "contract_number": "string | null",
    "contract_date": "string | null",
    "items": [
      {
        "description": "string",
        "quantity": "string",
        "unit": "string",
        "price": "string",
        "amount": "string"
      }
    ]
  },
  "ocr_text": "string | null",
}
```

#### MarkCreatedIn1CRequest
```json
{
  "external_id_1c": "string (required) - ID документа в системе 1С"
}
```

#### Create1CExpenseRequest
```json
{
  "invoice_id": "integer (required)",
  "document_number_1c": "string (required)",
  "document_date_1c": "string (ISO 8601, required)",
  "category_id": "integer (required)",
  "amount": "float (optional)",
  "comment": "string (optional)"
}
```

#### Create1CExpenseResponse
```json
{
  "success": "boolean",
  "expense_id": "integer",
  "expense_number": "string",
  "contractor_id": "integer | null",
  "contractor_created": "boolean",
  "message": "string"
}
```

### Коды ответов

- **200 OK** - Успешный запрос
- **201 Created** - Ресурс создан
- **400 Bad Request** - Неверные параметры запроса
- **401 Unauthorized** - Неверный или отсутствующий токен
- **403 Forbidden** - Недостаточно прав (проверьте scope токена)
- **404 Not Found** - Ресурс не найден
- **409 Conflict** - Заявка уже создана для этого счета
- **500 Internal Server Error** - Ошибка сервера

## 8. Workflow обработки счетов

### Вариант 1: Прямое создание заявки через API (legacy)
```
[1] Пользователь загружает PDF счет
         ↓
[2] OCR извлекает текст (Tesseract)
         ↓
[3] AI парсит данные (VseGPT)
         ↓
[4] Создается ProcessedInvoice (status: PROCESSED)
         ↓
[5] 1С запрашивает pending invoices (GET /pending)
         ↓
[6] 1С создает "Заявку на расходование" в своей базе
         ↓
[7] 1С отправляет данные документа в API (POST /create-expense)
         ↓
[8] API создает Expense в системе бюджетирования
         ↓
[9] API обновляет ProcessedInvoice (status: EXPENSE_CREATED)
         ↓
[10] Пользователь видит новую заявку в веб-интерфейсе
```

### Вариант 2: Только обмен данными (рекомендуется для новых интеграций)
```
[1] Пользователь загружает PDF счет
         ↓
[2] OCR извлекает текст (Tesseract)
         ↓
[3] AI парсит данные (VseGPT) с табличной частью
         ↓
[4] Создается ProcessedInvoice (status: PROCESSED)
         ↓
[5] 1С запрашивает pending invoices (GET /pending?only_not_created_in_1c=true)
         ↓
[6] 1С получает полные данные счета (GET /{invoice_id})
         ↓
[7] 1С создает документ в своей базе (используя parsed_data.items)
         ↓
[8] 1С отмечает счет как обработанный (POST /{invoice_id}/mark-created-in-1c)
         ↓
[9] ProcessedInvoice обновляется: external_id_1c + created_in_1c_at
         ↓
[10] Счет больше не возвращается при запросе pending с фильтром
         ↓
[11] Пользователь видит статус обработки в веб-интерфейсе
```

**Преимущества варианта 2:**
- ✅ Полная информация о счете с табличной частью
- ✅ Отслеживание статуса обработки в 1С
- ✅ Связь документов через external_id_1c
- ✅ Исключение дублирования при повторных запросах
- ✅ Гибкость в обработке данных на стороне 1С

## 9. Дополнительные возможности

### Фильтрация только новых счетов (рекомендуется)

Получить только счета, которые еще не были созданы в 1С:
```http
GET /api/v1/external/invoices/pending?only_not_created_in_1c=true&department_id=1&limit=50
```

**Преимущества:**
- Исключает повторную обработку уже созданных счетов
- Повышает производительность за счет меньшего объема данных
- Предотвращает дублирование документов в 1С

### Фильтрация по департаменту

При запросе списка счетов можно указать department_id:
```http
GET /api/v1/external/invoices/pending?department_id=1&limit=100
```

### Лимит записей

Ограничьте количество возвращаемых счетов:
```http
GET /api/v1/external/invoices/pending?limit=50
```

### Комбинированные фильтры

Можно комбинировать несколько фильтров:
```http
GET /api/v1/external/invoices/pending?department_id=1&only_not_created_in_1c=true&limit=25
```

### Работа с табличной частью

Получите полные данные счета включая позиции:
```http
GET /api/v1/external/invoices/123
```

Затем обработайте `parsed_data.items`:
```bsl
Процедура ОбработатьТабличнуюЧасть(ДанныеСчета)
    Если ДанныеСчета.parsed_data <> Неопределено Тогда
        Items = ДанныеСчета.parsed_data.Получить("items");

        Для Каждого Item Из Items Цикл
            Наименование = Item.Получить("description");
            Количество = Число(Item.Получить("quantity"));
            Цена = Число(Item.Получить("price"));
            Сумма = Число(Item.Получить("amount"));

            // Создать строку документа
            НоваяСтрока = ДокументОбъект.Товары.Добавить();
            НоваяСтрока.Номенклатура = НайтиИлиСоздатьНоменклатуру(Наименование);
            НоваяСтрока.Количество = Количество;
            НоваяСтрока.Цена = Цена;
            НоваяСтрока.Сумма = Сумма;
        КонецЦикла;
    КонецЕсли;
КонецПроцедуры
```

### Проверка статуса перед обработкой

Перед созданием заявки проверьте, не был ли счет уже обработан:
```bsl
Функция СчетУжеОбработан(ДанныеСчета)
    // Проверяем поле created_in_1c_at
    Если ДанныеСчета.created_in_1c_at <> Null Тогда
        Сообщить("Счет уже обработан в 1С: " + ДанныеСчета.external_id_1c);
        Возврат Истина;
    КонецЕсли;

    Возврат Ложь;
КонецФункции
```

## 10. Поддержка

При возникновении проблем:
1. Проверьте логи backend: `tail -f backend/logs/app.log`
2. Проверьте логи обработки в 1С
3. Проверьте статус счетов в веб-интерфейсе (раздел "Обработка счетов")
4. Обратитесь к разработчику с подробным описанием проблемы и логами
