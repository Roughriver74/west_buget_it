# Инструкция: Импорт бюджета склада

## 📊 Подготовка файлов

### Метод 1: Автоматическая подготовка (РЕКОМЕНДУЕТСЯ)

#### 1.1. Подготовка бюджета по расходам

```bash
cd backend
python3 scripts/prepare_warehouse_expenses.py
```

**Результат:**
- ✅ Создан файл: `xls/prepared/warehouse_expenses_2025_prepared.xlsx`
- 📊 408 записей (34 категории × 12 месяцев)
- 💰 Общая сумма: ~54,5 млн руб.

#### 1.2. Структура подготовленного файла

| Категория | Тип расходов | Год | Месяц | Плановая сумма | Обоснование |
|-----------|--------------|-----|-------|----------------|-------------|
| Вода питьевая... | OPEX | 2025 | 1 | 30000 | 360000 |
| Канцелярия... | OPEX | 2025 | 1 | 100000 | 1200000 |
| ... | ... | ... | ... | ... | ... |

### Метод 2: Ручная подготовка

**Исходный файл:** `Бюджет_склад_расходы_2025_готово.xlsx`

Этот файл уже в правильном формате и содержит 34 категории расходов.

### ⚠️ Файл зарплат

**Исходный файл:** `Бюджет ЗП склад 2025_3.xlsx`

Файл имеет сложную структуру. Рекомендуется импортировать зарплату через раздел **"Планирование ФОТ"** в веб-интерфейсе

---

## 🚀 Импорт через UI

### Шаг 1: Создание категорий

Перед импортом убедитесь, что **все категории** из Excel файла существуют в системе для отдела "Склад":

1. Откройте http://localhost:5173
2. Войдите в систему (admin/admin)
3. Перейдите в раздел **"Справочники" → "Категории бюджета"**
4. Выберите отдел "Склад" в фильтре
5. Создайте недостающие категории:
   - Вода питьевая бутилированная 19л.
   - Канцелярия + чай + кофе+ молоко
   - Бумага
   - Пухто - вывоз ломанных паллетов
   - Термоэтикетки маленькие
   - Термоэтикетки большие
   - ... и остальные 28 категорий из файла

### Шаг 2: Создание сценария и версии

1. Перейдите в **"Планирование бюджета"**
2. Выберите отдел "Склад" в шапке
3. Выберите год **2025**
4. Нажмите **"Создать сценарий"**:
   - Тип: Базовый
   - Название: "Базовый сценарий 2025"
   - Нажмите "ОК"

5. Выберите созданный сценарий
6. Нажмите **"Создать версию"**:
   - Название: "План расходов склада"
   - Нажмите "ОК"

### Шаг 3: Импорт данных

1. В таблице версий найдите созданную версию
2. Нажмите кнопку **"Просмотр"** (глаз) на версии
3. В открывшемся drawer справа найдите кнопку **"Импорт"** или **"Загрузить из Excel"**
4. Выберите файл: `xls/Бюджет_склад_расходы_2025_готово.xlsx`
5. Дождитесь загрузки
6. Проверьте импортированные данные

### Шаг 4: Утверждение и применение

1. Закройте drawer
2. Нажмите кнопку **"Отправить на согласование"** (конверт) на версии
3. Войдите под пользователем с ролью ADMIN или MANAGER
4. Нажмите кнопку **"Утвердить"** (зеленая галочка ✓)
5. Нажмите кнопку **"Применить к плану"** (зеленая галочка в круге)

✅ Бюджет применен к плану на 2025 год!

---

## 🔧 Метод 3: Прямой импорт на сервере (для больших объемов)

### Шаг 1: Копирование файлов на сервер

```bash
# С локального компьютера
scp xls/prepared/warehouse_expenses_2025_prepared.xlsx root@93.189.228.52:/root/import/
scp backend/scripts/import_warehouse_data.py root@93.189.228.52:/root/import/
```

### Шаг 2: Узнать ID отдела

```bash
ssh root@93.189.228.52

# Через PostgreSQL
docker exec -it it_budget_db psql -U budget_user -d it_budget_db \
  -c "SELECT id, name FROM departments;"
```

Пример результата:
```
 id |     name
----+--------------
  1 | IT
  2 | Склад
  3 | Продажи
```

Запомните ID отдела "Склад" (например, `2`).

### Шаг 3: Запуск импорта

```bash
cd /root/it-budget-manager  # или путь к проекту

# Активировать виртуальное окружение
source venv/bin/activate

# Запустить импорт
python scripts/import_warehouse_data.py \
  --file /root/import/warehouse_expenses_2025_prepared.xlsx \
  --department-id 2 \
  --type expenses
```

**Ожидаемый результат:**
```
================================================================================
✅ ИМПОРТ ЗАВЕРШЕН
================================================================================

📊 Статистика:
  - Создано новых записей: 408
  - Обновлено записей: 0
  - Всего обработано: 408
```

### Шаг 4: Проверка

```sql
-- Проверить общую сумму бюджета
SELECT SUM(planned_amount) AS total_budget
FROM budget_plan_details bpd
JOIN budget_plans bp ON bpd.budget_plan_id = bp.id
WHERE bp.department_id = 2 AND bp.year = 2025;
```

Ожидаемый результат: `54,457,933.00`

---

## 🔧 Метод 4: Импорт через API

Если UI кнопка не работает, можно использовать curl:

```bash
# 1. Получите токен (замените admin:admin на свои данные)
TOKEN=$(curl -X POST "http://localhost:8000/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin" | jq -r '.access_token')

# 2. Найдите ID версии (замените 1 на ID отдела Склад)
curl -X GET "http://localhost:8000/api/v1/budget/planning/versions?year=2025" \
  -H "Authorization: Bearer $TOKEN"

# 3. Импортируйте файл (замените VERSION_ID на реальный ID)
curl -X POST "http://localhost:8000/api/v1/budget/planning/versions/VERSION_ID/import" \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@xls/Бюджет_склад_расходы_2025_готово.xlsx"
```

---

## 📝 Список категорий в файле

Файл `Бюджет_склад_расходы_2025_готово.xlsx` содержит следующие категории:

1. Вода питьевая бутилированная 19л.
2. Канцелярия + чай + кофе+ молоко
3. Бумага
4. Пухто - вывоз ломанных паллетов и отбрешоток от грузов + вывоз ТБО
5. Термоэтикетки маленькие
6. Термоэтикетки большие
7. Ролик для тату
8. Пленка для паллет 500мм (17.5мкм) прозрачная
9. Пленка для паллет 500мм вторичка (17.5 мкм) черная
10. Риббон Термотрансферная лента  170 х 300
11. Маркеры
12. Риббон Термотрансферная лента  57 х 300
13. Кант лист 100% полиэстер 2*3 м
14. Мойка складского помещения уборка туалета
15. Уборка мусора на складах  и мойка бордюров
16. Круглосуточное хранение спецтехники
17. Одноразовые станки
18. Салфетки бумажные листовые складные
19. Бумага туалетная в рулонах
20. Дозатор для жидкого мыла
21. Средство для мытья полов  Я чистый пол
22. Туалетное мыло жидкое антибактериальное
23. Мусорные пакеты 120л
24. Мусорные пакеты 60л
25. Лопаты
26. Пломбы-индикаторы
27. Перчатки резиновые ВЕНЕС
28. Рулетка измерительная 3 метра
29. Доводчики для складских дверей
30. Мешки для мусора 240л
31. Коврик резиновый
32. Арендная плата за складское помещение (Интерскол ДВ)
33. Уборка складского помещения АВБ (туалет-уборка)
34. Охрана помещения АВБ

**Все категории должны быть созданы в системе для отдела "Склад" с типом OPEX.**

---

## ❓ Проблемы и решения

### "Категория не найдена"
- Создайте категорию в справочнике
- Убедитесь, что название точно совпадает (включая пробелы)
- Проверьте выбранный отдел

### "Версия не найдена"
- Убедитесь, что выбран правильный отдел в шапке
- Создайте сценарий и версию заново

### "Файл слишком большой"
- Максимальный размер: 10 MB
- Удалите лишние столбцы/форматирование

### Кнопка импорта не работает
- Используйте API метод (см. выше)
- Обновите страницу (Ctrl+R)
- Проверьте консоль браузера (F12)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи backend: `tail -f backend.log`
2. Проверьте логи frontend: `tail -f frontend.log`
3. Проверьте консоль браузера (F12)
