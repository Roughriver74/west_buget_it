# 🚀 Быстрый старт: Импорт данных склада

## Шаг 1: Подготовка данных (локально)

```bash
cd backend
python3 scripts/prepare_warehouse_expenses.py
```

**Результат:**
- Файл `xls/prepared/warehouse_expenses_2025_prepared.xlsx`
- 408 записей, ~54.5 млн руб.

---

## Шаг 2: Копирование на сервер

```bash
scp xls/prepared/warehouse_expenses_2025_prepared.xlsx root@93.189.228.52:/root/import/
scp backend/scripts/import_warehouse_data.py root@93.189.228.52:/root/import/
```

---

## Шаг 3: Импорт на сервере

### 3.1. Подключиться к серверу

```bash
ssh root@93.189.228.52
```

### 3.2. Узнать ID отдела "Склад"

```bash
docker exec -it it_budget_db psql -U budget_user -d it_budget_db \
  -c "SELECT id, name FROM departments;"
```

**Пример:** ID отдела "Склад" = `2`

### 3.3. Запустить импорт

```bash
cd /root/it-budget-manager  # путь к вашему проекту
source venv/bin/activate     # если используется venv

python scripts/import_warehouse_data.py \
  --file /root/import/warehouse_expenses_2025_prepared.xlsx \
  --department-id 2 \
  --type expenses
```

---

## Шаг 4: Проверка

### Через веб-интерфейс

1. Откройте http://93.189.228.52:5173
2. Войдите как ADMIN
3. Перейдите в **"Бюджетное планирование"**
4. Выберите отдел **"Склад"**
5. Выберите год **2025**
6. Проверьте данные

### Через PostgreSQL

```bash
docker exec -it it_budget_db psql -U budget_user -d it_budget_db

-- Проверить общую сумму
SELECT SUM(planned_amount) AS total_budget
FROM budget_plan_details bpd
JOIN budget_plans bp ON bpd.budget_plan_id = bp.id
WHERE bp.department_id = 2 AND bp.year = 2025;
```

**Ожидаемый результат:** `54,457,933.00`

---

## ✅ Готово!

Данные успешно импортированы. Теперь можно:
- Анализировать бюджет по категориям
- Сравнивать план/факт
- Создавать отчеты

---

## 📚 Дополнительно

- **Полная инструкция:** [ИНСТРУКЦИЯ_ИМПОРТ_СКЛАД.md](ИНСТРУКЦИЯ_ИМПОРТ_СКЛАД.md)
- **Импорт зарплаты:** Через веб-интерфейс в разделе "Планирование ФОТ"
- **Техническая документация:** [docs/API_DATA_IMPORT.md](../docs/API_DATA_IMPORT.md)
