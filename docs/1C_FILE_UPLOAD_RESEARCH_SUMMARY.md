# Исследование загрузки файлов в 1С через OData

## Дата: 2025-11-20

## Цель
Реализовать загрузку файлов (счетов, накладных) к заявкам на расход в 1С через OData API.

## Исследованные подходы

### ❌ Подход 1: InformationRegister_ПрисоединенныеФайлы
**Статус**: НЕ РАБОТАЕТ

**Результат**: Endpoint не существует в данной конфигурации 1С
```
HTTP 404: Сущность 'InformationRegister_ПрисоединенныеФайлы' не найдена
```

---

### ❌ Подход 2: Catalog_Файлы с владельцем Document_ЗаявкаНаРасходованиеДенежныхСредств
**Статус**: НЕ ПОДДЕРЖИВАЕТСЯ

**Результат**: Catalog_Файлы не поддерживает заявки на расход как владельца

**Допустимые владельцы**:
- `BusinessProcess_Задание`
- `Catalog_ПапкиФайлов`
- `Document_ОтчетОРозничныхПродажах`
- `Document_ОтчетОРозничныхВозвратах`
- `Catalog_ФизическиеЛица`

**Ошибка**:
```json
{
  "code": "4",
  "message": {
    "value": "Недопустимое значение 'StandardODATA.Document_ЗаявкаНаРасходованиеДенежныхСредств'
              для свойства 'ВладелецФайла' составного типа"
  }
}
```

---

### ❌ Подход 3: Catalog_Файлы без владельца
**Статус**: ВНУТРЕННЯЯ ОШИБКА 1С

**Результат**: 500 Internal server error при попытке создания

**Попытки**:
1. ✅ Минимальный набор полей: `Description`, `ФайлХранилище_Base64Data`, `ФайлХранилище_Type`, `Расширение`
2. ✅ С дополнительными полями: `Размер`, `ДатаСоздания`, `ДатаМодификацииУниверсальная`
3. ✅ С владельцем = `Catalog_ПапкиФайлов` (папка файлов)

**Ошибка**:
```json
{
  "code": "-1",
  "message": {
    "value": "Произошла внутренняя ошибка OData сервиса.
              Дополнительные сведения можно найти в технологическом журнале."
  }
}
```

**Причина**: Бизнес-логика 1С (обработчик `ПередЗаписью`) отклоняет запрос.
Для выяснения точной причины нужен доступ к технологическому журналу 1С.

---

### ✅ Подход 4: Табличная часть ПодтверждающиеДокументы (ТЕОРЕТИЧЕСКИ ВЕРНЫЙ)
**Статус**: ПРАВИЛЬНЫЙ ПОДХОД, НО НЕ РЕАЛИЗОВАН ИЗ-ЗА БЛОКА В ПОДХОДЕ 3

**Теория**:
1. Создать файл в `Catalog_Файлы` (получить `Ref_Key`)
2. Добавить запись в табличную часть `ПодтверждающиеДокументы` заявки с ссылкой на файл

**Структура табличной части**:
```xml
<ComplexType Name="Document_ЗаявкаНаРасходованиеДенежныхСредств_ПодтверждающиеДокументы_RowType">
  <Property Name="Ref_Key" Type="Edm.Guid" Nullable="false"/>
  <Property Name="LineNumber" Type="Edm.Int64" Nullable="false"/>
  <Property Name="ВидДокумента" Type="Edm.String" Nullable="true"/>
  <Property Name="Номер" Type="Edm.String" Nullable="true"/>
  <Property Name="Дата" Type="Edm.DateTime" Nullable="true"/>
  <Property Name="Сумма" Type="Edm.Double" Nullable="true"/>
  <Property Name="Файл" Type="Edm.String" Nullable="true"/>         ← ССЫЛКА НА ФАЙЛ
  <Property Name="Файл_Type" Type="Edm.String" Nullable="true"/>   ← ТИП ССЫЛКИ
</ComplexType>
```

**Пример данных**:
```json
{
  "LineNumber": 1,
  "ВидДокумента": "Счет",
  "Номер": "test_invoice_20251120.pdf",
  "Дата": "2025-11-20T00:00:00",
  "Сумма": 50000.00,
  "Файл": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "Файл_Type": "StandardODATA.Catalog_Файлы"
}
```

**Блокер**: Невозможно создать файл в `Catalog_Файлы` (подход 3 не работает)

---

## Выводы и рекомендации

### ❌ Что НЕ работает через OData:
1. Создание файлов в `Catalog_Файлы` - блокируется бизнес-логикой 1С
2. Прямое прикрепление файлов к `Document_ЗаявкаНаРасходованиеДенежныхСредств`

### ✅ Что работает:
1. Получение существующих файлов из `Catalog_Файлы` (GET)
2. Получение метаданных и структуры через `$metadata`
3. Работа с заявками на расход (CRUD)

### 💡 Альтернативные решения:

#### Вариант 1: Хранение файлов в нашей БД + метаданные для синхронизации
**Подход**:
- Файлы хранятся в `attachments` таблице IT Budget Manager
- При синхронизации с 1С передаем только метаданные (имя файла, размер)
- Ссылка на файл - внешний URL из IT Budget Manager

**Преимущества**:
- ✅ Полный контроль над файлами
- ✅ Не зависим от ограничений 1С OData
- ✅ Быстрый доступ к файлам

**Недостатки**:
- ❌ Файлы не физически в 1С
- ❌ Нужна дополнительная логика синхронизации

#### Вариант 2: Использование внешних обработок 1С
**Подход**:
- Разработать внешнюю обработку в 1С для загрузки файлов
- Вызывать через HTTP-сервис 1С (не OData)

**Преимущества**:
- ✅ Обход ограничений OData
- ✅ Полный доступ к API 1С

**Недостатки**:
- ❌ Требуется разработка на стороне 1С
- ❌ Нужны права администратора 1С

#### Вариант 3: FTP/File System интеграция
**Подход**:
- Сохранять файлы в файловую систему сервера 1С
- Регистрировать в 1С через планировщик заданий

**Преимущества**:
- ✅ Не требует изменений в OData
- ✅ Файлы физически в 1С

**Недостатки**:
- ❌ Нужен доступ к файловой системе сервера 1С
- ❌ Сложная настройка

#### ⭐ Вариант 4: Обращение к администратору 1С (РЕКОМЕНДУЕТСЯ)
**Подход**:
1. Запросить у администратора 1С доступ к технологическому журналу
2. Выяснить точную причину ошибки при создании файлов
3. Попросить настроить права OData для создания файлов
4. Возможно потребуется обновить бизнес-логику обработчика `ПередЗаписью`

**Преимущества**:
- ✅ Решение в корне проблемы
- ✅ Использование штатного механизма 1С
- ✅ Интеграция работает как задумано

**Недостатки**:
- ❌ Требуется помощь администратора 1С
- ❌ Возможны изменения в конфигурации 1С

---

## Технические детали

### Существующие Catalog для прикрепленных файлов:
- `Catalog_ДоговорыКонтрагентовПрисоединенныеФайлы` - файлы договоров
- `Catalog_НоменклатураПрисоединенныеФайлы` - файлы номенклатуры
- `Catalog_ОрганизацииПрисоединенныеФайлы` - файлы организаций
- `Catalog_СообщениеЭДОПрисоединенныеФайлы` - файлы ЭДО

**Важно**: Для заявок на расход такого catalog НЕТ.

### Проверенные endpoints:
- ❌ `InformationRegister_ПрисоединенныеФайлы` - не существует
- ✅ `Catalog_Файлы` - существует, но создание блокируется
- ✅ `Catalog_ВерсииФайлов` - существует (для версий файлов)
- ✅ `Catalog_ПапкиФайлов` - существует (папки файлов)

### Ключевые поля для BASE64:
- `ФайлХранилище_Base64Data` - бинарные данные в BASE64
- `ФайлХранилище_Type` - **ОБЯЗАТЕЛЬНОЕ** поле с MIME-типом (например: `application/pdf`)
- `Расширение` - расширение файла без точки (например: `pdf`)

---

## Скрипты исследования

Созданы следующие тестовые скрипты в `backend/scripts/`:

1. `find_file_endpoints.py` - поиск endpoints для файлов
2. `inspect_catalog_files.py` - проверка структуры Catalog_Файлы
3. `find_expense_file_catalog.py` - поиск специального catalog для заявок
4. `inspect_expense_document_structure.py` - структура документа заявки
5. `inspect_supporting_docs_structure.py` - структура табличной части ПодтверждающиеДокументы
6. `test_file_upload_catalog.py` - тест загрузки с правильным endpoint
7. `test_file_upload_correct.py` - тест с правильной структурой полей
8. `test_file_with_folder.py` - тест с использованием папки как владельца
9. `test_complete_file_upload_solution.py` - полное решение (теоретическое)

---

## Следующие шаги

### Краткосрочные:
1. ✅ Обновить endpoint по умолчанию в конфиге: `Catalog_Файлы`
2. ✅ Задокументировать найденную структуру
3. ⏳ Связаться с администратором 1С для выяснения причин блокировки

### Долгосрочные:
1. ⏳ Реализовать Вариант 1 (хранение в IT Budget Manager)
2. ⏳ При получении доступа - реализовать Подход 4 (табличная часть)

---

## Контакты и доступы

**1C OData API**:
- URL: `http://10.10.100.77/trade/odata/standard.odata`
- User: `odata.user`
- Конфигурация: "Управление торговлей" (версия неизвестна)

**Для решения проблемы нужно**:
- Доступ к технологическому журналу 1С
- Консультация с администратором 1С
- Возможно, настройка прав доступа OData

---

## Обновления кода

### Изменено:
- `backend/app/core/config.py`:
  - ✅ Изменен `ODATA_1C_ATTACHMENT_ENDPOINT` с `InformationRegister_ПрисоединенныеФайлы` на `Catalog_Файлы`
  - ✅ Добавлен `ODATA_1C_MAX_FILE_SIZE_MB` для контроля размера файлов

- `backend/.env.example`:
  - ✅ Обновлен пример конфигурации OData

- `backend/app/services/odata_1c_client.py`:
  - ✅ Улучшен метод `upload_attachment_base64`:
    - Поддержка configurable endpoint
    - Проверка размера файла
    - Расширенное логирование
    - Fallback на дефолтные значения

### Не изменено (работает как и раньше):
- Получение данных из 1С (заявки, организации, контрагенты)
- Создание заявок на расход
- Синхронизация справочников

---

## Заключение

**Загрузка файлов в 1С через OData в текущей конфигурации НЕВОЗМОЖНА** из-за ограничений бизнес-логики 1С.

**Рекомендуемое решение**: Обратиться к администратору 1С для настройки доступа или использовать альтернативный Вариант 1 (хранение файлов в IT Budget Manager).

Все исследование задокументировано, тестовые скрипты сохранены для дальнейшего использования при получении доступа.
